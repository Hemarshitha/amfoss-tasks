#include <iostream>
#include <string>

int main() {
    int n;
    std::cout << "Enter an odd number: ";
    std::cin >> n;

    // Upper part
    for (int i = 0; i <= n / 2; i++) {
    }

    // Lower part
    for (int i = n / 2 - 1; i >= 0; i--) {
    }
    return 0;
}
